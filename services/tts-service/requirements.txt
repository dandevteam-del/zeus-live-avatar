TTS>=0.22.0
websockets>=12.0
numpy>=1.24
redis>=5.0
pydantic>=2.0
soundfile>=0.12
# Piper TTS (optional — only needed if TTS_ENGINE=piper)
# Install manually: pip install piper-tts
# Or from source: pip install git+https://github.com/rhasspy/piper.git
